services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: MVC_DB
      POSTGRES_USER: mvc
      POSTGRES_PASSWORD: postgres
    ports:
      - "5450:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: